# Dot configurations

## Installation

```bash
mv ~/.config ~/.config.bak
git clone https://github.com/ZachVec/dot.git ~/.config

# Linux
ln -sT ~/.config/zsh/zshrc.zsh ~/.zshrc

# Mac
ln -s ~/.config/zsh/zshrc.zsh ~/.zshrc
```
