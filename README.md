# Dot configurations

## Installation

```bash
git clone https://github.com/ZachVec/dot.git ~/.config
~/.config/setup.sh
```

`setup.sh` auto-detects macOS/Linux and prompts for bash or zsh. It creates the
necessary symlinks (backing up existing files) and initializes git submodules.
